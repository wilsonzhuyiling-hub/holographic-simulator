"""Holographic Geometric Framework — model core.

This package contains the legacy event/constant generator inherited from the
v1.0 prototype. It computes derived constants, particle events, force
validations, and DM relaxation tables from the framework axioms.

The new dynamics engine (sibling package `dynamics/`) builds on top of this
core to add ODE integration, automatic critical-point detection, and lightcone
projection.
"""
